/*
using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class TestScript
{

    Game game;

    [UnityTest]
    public IEnumerator TestScriptWithEnumeratorPasses()
    {
        GameObject gameGameObject = MonoBehaviour.Instantiate(Resources.Load<GameObject>("Prefabs/Game"));

        //Spawn the asteroid be getting the spawner
        /*
         game = gameObjectObject.GetComponent<Game>();

        SpAWNER SPAWNER = GAME.gETsPAWNER();

        float initiaLpOS = ASTEROIDS.TRANSFORM.POSITION.Y 
         
         YIELD RETURN NEW WAITFORSEOCNDS (.5]

Assert.Less(asteroid.transform.position.y, initialPosition);

        Object.Destroy(game.gameObject);

    }
}

*/